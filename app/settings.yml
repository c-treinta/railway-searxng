use_default_settings: true
server:
  secret_key: "SEARXNG_SECRET_KEY_PLACEHOLDER"
  bind_address: "0.0.0.0:BIND_PORT_PLACEHOLDER"
  image_proxy: true
  # ADMIN_PASSWORD_PLACEHOLDER
search:
  default_lang: "en"
ui:
  default_theme: simple
